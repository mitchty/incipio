language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get -qq update

before_script:
  - ./autogen.sh

script:
  - ./configure || { cat config.log; false; }
  - make distcheck

notifications:
  email:
    - mitchty.travis@gmail.com
